<%= form_for(@pfragment) do |f| %>
  <% if @pfragment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pfragment.errors.count, "error") %> prohibited this pfragment from being saved:</h2>

      <ul>
      <% @pfragment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :markup %><br />
    <%= f.text_area :markup %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
